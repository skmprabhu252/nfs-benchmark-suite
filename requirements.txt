# NFS Performance Test Suite - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
pyyaml>=5.4.1  # Compatible with Python 3.6+
jsonschema>=3.2.0,<4.0  # jsonschema 4.x requires Python 3.7+, using 3.x for Python 3.6 compatibility

# System metrics collection (optional but recommended)
psutil>=5.8.0  # Compatible with Python 3.6+

# HTML report generation (optional)
plotly>=4.14.3  # Compatible with Python 3.6+, allows newer versions if already installed
jinja2>=2.11.3  # Compatible with Python 3.6+, allows newer versions if already installed